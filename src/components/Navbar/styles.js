// Styles
import { heightNavigationBar } from '../../styles/sizes';
import {
  brownDark,
  turquoise,
  white,
} from '../../styles/colors';

export const statusBar = {
  style: 'light-content',
  tintColor: brownDark,
};

export const navbarStyle = {
  borderColor: turquoise,
  borderTopWidth: 5,
  height: heightNavigationBar,
};

export const tintColor = white;
