// Externals
import styled from 'styled-components/native';

// Config
import {
  heightContent,
  widthWindow,
} from '../../config/sizes';

export default styled.Image`
  align-self: center;
  height: ${heightContent};
  width: ${widthWindow};
`;
