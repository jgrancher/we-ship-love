// Externals
import styled from 'styled-components/native';

// Shared
import {
  heightContent,
  widthWindow,
} from '../../shared/sizes';

export default styled.Image`
  align-self: center;
  height: ${heightContent};
  width: ${widthWindow};
`;
