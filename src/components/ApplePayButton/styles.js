// Externals
import styled from 'styled-components/native';

// Shared
import { grey } from '../../shared/colors';

export default styled.Text`
  color: ${grey};
  font-size: 10;
  position: absolute;
  text-align: center;
  top: -20;
  width: 124;
`;
